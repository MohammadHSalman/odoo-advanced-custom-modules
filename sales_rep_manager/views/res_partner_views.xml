<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_partner_tree_inh" model="ir.ui.view">
        <field name="name">view partner tree inh</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='user_id']" position="replace">
                <field name="user_id" optional="show" widget="many2one_avatar_user"/>
            </xpath>

        </field>
    </record>
    <!-- Inherit Form View to Modify it -->
    <record id="view_partner_form_inh" model="ir.ui.view">
        <field name="name">view partner form inh</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='user_id']" position="replace">
                <field name="user_id" optional="show" widget="many2one_avatar_user"/>
            </xpath>
            <xpath expr="//sheet//field[@name='category_id']" position="after">
                <field name="customer_classification" options="{'no_create': True}"/>
            </xpath>


        </field>
    </record>
</odoo>